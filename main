<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SoundFlow</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #f5f1e6; /* light beige */
      --shadow-light: #ffffff;
      --shadow-dark: #c8c8c8;
      --text: #333;
    }

    * {
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: var(--bg);
      color: var(--text);
      margin: 0;
      padding: 2rem;
    }

    .container {
      max-width: 960px;
      margin: 0 auto;
    }

    .card {
      background: var(--bg);
      border-radius: 20px;
      box-shadow: 8px 8px 16px var(--shadow-dark), -8px -8px 16px var(--shadow-light);
      padding: 2rem;
      margin-bottom: 2rem;
    }

    .button {
      background: var(--bg);
      border: none;
      border-radius: 12px;
      box-shadow: 4px 4px 8px var(--shadow-dark), -4px -4px 8px var(--shadow-light);
      padding: 1rem 2rem;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.2s ease-in-out;
      text-decoration: none;
      color: var(--text);
    }

    .button:hover {
      box-shadow: inset 4px 4px 8px var(--shadow-dark), inset -4px -4px 8px var(--shadow-light);
    }

    h1 {
      text-align: center;
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    p {
      font-size: 1rem;
      line-height: 1.6;
      word-break: break-word;
    }

    a {
      color: #1a0dab;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card">
      <h1>Welcome to SoundFlow</h1>
      <p>This is my page where you can find some info about me.</p>
    </div>

    <div class="card">
      <h2>About</h2>
      <p>*info about me*</p>
    </div>

    <div class="card">
      <h2>Contact</h2>
      <p>Direct Links: 
      <a href="https://www.instagram.com/samuel_and_art?igsh=MWdiaTQ5cGl4cDRnbw==" target="_blank">Visit my Instagram</a>
      </p>
    </div>
  </div>
</body>
</html>
